# Intraday accumulation archive — immutable parquet day files are LFS-tracked
# (see data/intraday/README.md: the archive is the durable research record)
data/intraday/raw/** filter=lfs diff=lfs merge=lfs -text
